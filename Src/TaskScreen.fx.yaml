TaskScreen As screen:

    Button5 As button:
        Height: =70
        OnSelect: |-
            =Set(
                task_id,
                Defaults(B_TASK).B_TASK
            );
            Navigate(TaskEditScreen,ScreenTransition.None);
        Size: =24
        Text: ="タスクを追加"
        Width: =280
        X: =56
        ZIndex: =1

