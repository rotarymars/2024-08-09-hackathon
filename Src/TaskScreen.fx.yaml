TaskScreen As screen:

    Button5 As button:
        Height: =70
        OnSelect: |-
            =Set(
                task_id,
                Blank()
            );
            Navigate(TaskEditScreen,ScreenTransition.None);
        Size: =24
        Text: ="タスクを追加"
        Width: =280
        ZIndex: =1

